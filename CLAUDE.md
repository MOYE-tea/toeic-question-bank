# toeic-question-bank

多益（TOEIC）閱讀（RC）大量題庫練習網站。純前端靜態網站，無需伺服器，雙擊 `index.html` 或用瀏覽器開啟 file:// 路徑即可使用。所有進度（作答紀錄、錯題本、模考歷史）存在瀏覽器 localStorage，換瀏覽器/清快取會遺失。

## 目標與現況

使用者目前多益約 200 分，目標三個月內衝到 990（申請台大用）。目前題庫只涵蓋 **閱讀（RC: Part 5/6/7）**，聽力（LC）尚未開發。

## 檔案結構

- `index.html` — 單頁應用外殼，分頁式導覽（儀表板 / 練習 / 模擬考 / 單字 / 錯題本 / 匯入題庫）
- `styles.css` — 樣式
- `app.js` — 全部邏輯（無框架、純 vanilla JS）
- `data/part5.js` / `part6.js` / `part7.js` — RC 題庫本體，各自把 JSON 陣列指派給 `window.TOEIC_PART5` / `TOEIC_PART6` / `TOEIC_PART7`（用 `.js` 包成全域變數而非 `.json` + fetch，是為了讓網頁可以直接用 `file://` 雙擊打開，不會撞到瀏覽器對本機檔案 fetch 的 CORS 限制）
- `data/vocab.js` — 多益商用高頻單字庫本體，指派給 `window.TOEIC_VOCAB`
- `data/hs7000.js` — 高中英文參考詞彙表（俗稱「高中7000單」）本體，指派給 `window.HS7000_VOCAB`；目前只做了 Level 1（見下方）

## 題庫規模（2026-09-17）

- Part 5（句子文法/字彙單選）：60 題，難度 1-5 分層，涵蓋時態、主詞動詞一致、介系詞、關係子句、假設語氣、倒裝、精確詞義辨析等文法點
- Part 6（段落填空）：6 篇 × 4 格 = 24 格
- Part 7（閱讀測驗）：單篇 6 組 + 雙篇 3 組 + 三篇 1 組 = 38 小題
- 多益商用高頻單字：150 個，涵蓋 10 大商用主題（會議簡報、人資徵才、財務會計、行銷業務、採購供應鏈、辦公室日常、契約法務、差旅住宿、科技IT、不動產設施），每字附詞性/中文意思/例句/難度
- 高中7000單：**Level 1 已 100% 完成（官方 1,026 字全部涵蓋）**。**這是真的官方字表**——使用者提供了 Google Drive 上的官方 PDF（《高中英文參考詞彙表》，75 頁，Level 1-6 各 1,080 字），用 `pdftotext`（無 `-layout`）抓出文字後寫 Python 解析出「單字＋詞性」（過程細節見下方「PDF 解析血淚史」），但 **PDF 完全沒有中文意思或例句**，那些是我自己另外寫的。所以：單字本身＝官方 Level 1 字表，中文意思/例句＝AI 原創，已用程式驗證每個官方單字都有對應條目、無重複 id、無重複單字（`may` 月份名詞和 `may` 助動詞這兩個故意留成兩筆不同條目，因為大小寫不同、詞性/意思完全不同）、例句都抓得到單字本身（含邊界檢查，避免像 `his` 誤配到 `This` 裡面這種問題，過程中真的抓到過幾個這樣的 bug 並修掉了）。**Level 2-6 完全還沒做**，PDF 原始檔和所有解析用的中間檔案都在對話當次的 scratchpad，沒有留在專案裡——如果要繼續做 Level 2+，需要使用者重新提供那個 Google Drive 連結（`drive.google.com/file/d/17gXN1NSXtSx0lNXEOpu6Ehh_Cuwqklee`，除非連結失效）。使用者已知情這是分批進行，說「繼續」目前的意思應該是接著做 Level 2

### PDF 解析血淚史（給之後要處理 Level 2-6 的人看）

Google Drive 檔案要用 `https://drive.google.com/uc?export=download&id=<FILE_ID>` 這種直連網址下載（`curl`），不能直接讀 `.../view` 分享頁。下載下來是 75 頁 PDF，`pdftotext -layout` 因為原文是雙欄排版，會把左右兩欄的文字交錯讀成亂序（同一行混進兩個不相干的字），**改用 `pdftotext`（不加 `-layout`）** 才會照 PDF 內部文字繪製順序輸出，剛好等於「先讀完左欄再讀右欄」，維持字母順序正確。

拿到的文字裡，每個詞條的音標（IPA）用自訂字型畫出來，`pdftotext` 抓不出正確 Unicode，會變亂碼——**音標整個丟棄即可，不影響單字/詞性判讀**。解析邏輯是拿 `[亂碼音標]` 方括號本身當「分隔符」：一個詞條 = 方括號前的文字（單字）+ 方括號後、下一個方括號前的文字（詞性，例如 `n.` `vt.` `adj.`）。踩過的坑：
- 詞性後面常直接黏著下一個單字（例如 `n. grandfather`），要先辨識出「已知詞性縮寫」再切開，不能整段當一體
- 詞性偶爾用 `.` 連續黏在一起沒空格（`vt.vi.n.`）或跟省略號黏在一起（`prep....`），要用正則個別拆解
- 標準單字 `art`（冠詞的名詞本身）跟詞性縮寫 `art.`（article 的縮寫）長得幾乎一樣，一定要要求詞性縮寫**必須有句點**才算數，不然像 "around... art[rt] n." 這種會把單字 "art" 誤判成詞性
- 清完上述規則後仍會零星殘留「詞性殘渣黏在單字前面」的詞條（例如 `n week` 應該是 `week`），最後用一個保險規則處理：清過一輪後單字裡若還有空白且不是已知的多字片語（如 `according to`），就只取最後一個空白分隔的字當作真正單字
- 全部清完後，Level 1 驗證結果是 1,026 個不重複單字（官方標榜 1,080，缺口可能是原始 PDF 排版本身就有一些條目沒有方括號音標，被我的分隔邏輯漏掉——如果之後要湊到剛好 1,080，需要再回頭比對）
- 有 4 個字官方字表本身就重複列了兩次、詞性不同（`may`／`shop`／`store`／`state`，例如名詞義和動詞義分開列），之後補這幾個字時要合併成一筆（`pos` 寫 `"n./vt."` 這種組合），不要真的建立兩筆重複的 `word`
- 兩份單字庫在同一個「單字」分頁裡合併使用（`getVocabList()` 會把 `TOEIC_VOCAB` 和 `HS7000_VOCAB` 陣列合併），分類篩選下拉選單會同時列出商用主題和「高中英文 Level 1」，使用者可以自己選要練哪一組。**測驗題不是預先寫好的，是從單字庫即時產生**（見下方「單字測驗題生成邏輯」），對兩份單字庫通用

全部內容為 AI 原創（比照多益真實題型與難度分布編寫），**非官方多益題目**，不涉及版權問題。若要擴充 RC 題庫量，直接比照 `data/part5.js` 等檔案的既有 schema 新增陣列項目即可（見下方 schema）；擴充單字庫則比照 `data/vocab.js` 的 schema。

## 資料 Schema

**Part 5**（`{id, difficulty(1-5), category, sentence, options[4], answer(0-3), explanation}`）

**Part 6**（`{id, difficulty, category, title, passage, blanks: [{options[4], answer, explanation} x N]}`）— `passage` 字串中用 `(1)_____`、`(2)_____`... 標記空格位置，`blanks` 陣列順序需對應。

**Part 7**（`{id, difficulty, category, passages: [{label, text}] (單篇1個/雙篇2個/三篇3個), questions: [{stem, options[4], answer, explanation} x N]}`）

**單字**（`{id, word, pos, meaning, example, category, difficulty(1-5)}`）— `example` 必須包含 `word` 的原形字串（大小寫不拘），否則例句填空題型會自動退回改考字義題型（見下方生成邏輯）。新增單字時務必檢查這點，否則該字只會出字義題。

**寫例句時要注意的陷阱（寫高中7000單 Level 1 時踩過的坑）**：app.js 的挖空邏輯是拿 `word` 字串去 `example` 裡做「不分大小寫、找第一個出現位置」的字串替換，**不是真正的整字比對**。兩種常見錯誤：
1. 單字剛好是另一個字的字尾子字串，例如 `his` 出現在 `This` 裡面（`T-his`）——如果例句用了 "This is his book."，程式會誤把 "This" 挖掉一部分，而不是挖真正那個 "his"。寫例句前要檢查目標單字有沒有可能是句子裡**更早出現**的其他字的子字串。
2. 單字被複數/時態變化（`hurt`→`hurts`、`happen`→`happened`）——這種**字尾加東西**的情況其實還好，程式挖空後會變成 "_____s" 這種樣子，讀得懂但不完美，非必要但盡量避免。
若不確定，比照這次用的驗證邏輯（正則找到第一個相符位置，檢查前後字元是否為英文字母，若前後緊鄰字母就是誤判）先跑過一次再匯入。

使用者也可以透過網站「匯入題庫」分頁貼上符合上述 schema 的 JSON 陣列（`id` 欄位會被自動覆寫產生，其餘欄位需齊全，網站會做基本格式驗證並跳過不符合的項目）。匯入的題目存在 localStorage：RC 題目存 `toeic_custom_part5/6/7`，單字存 `toeic_vocab_custom`；可用「匯出目前已匯入題目」下載備份。

## 功能設計要點

- **練習模式**（RC only）：依 Part 5/6/7 / 分類 / 難度 / 是否只出錯題本篩選，逐題作答＋即時對答案＋詳解
- **模擬考模式**（RC only）：自訂 Part5/6/7 題量與時間限制，全部作答完才統一計分（模擬真實考試），交卷後有 Part 別正確率拆解＋估算 RC 分數
- **估算分數**：`estimateScaledScore()` 用一組近似的答對率→量尺分數對照錨點做線性內插，**只是粗略估算，不是官方多益換算表**（官方換算表每場考試都會依難度做等化調整，外部無法精確重現）
- **錯題本**（RC only）：答錯自動加入；同一小題連續答對 2 次自動移出；可一鍵「複習全部錯題」進入練習模式
- **儀表板**（RC only）：累積題數/正確率/連續練習天數（用本地時間而非 UTC 計算，之前修過一次 UTC 時區導致跨日算錯的 bug）、各 Part 正確率、最弱分類（至少作答 3 次才納入排序）、模考分數趨勢
- **單字分頁**：獨立於上述 RC 練習/模考/錯題本/儀表板系統之外，自成一套（見下方）

### 單字分頁（獨立系統，2026-09-17 新增）

刻意設計成跟 RC 的 Practice/Mock/Wrongbook/Dashboard **不共用** 資料或 UI 流程，原因：單字題是即時生成的（見下方生成邏輯），若硬塞進以固定題目 id 為基礎的 RC 引擎（`findUnit`／`buildUnits` 等）需要額外處理「錯題本重新出題時要能還原成同一題」的問題，拆開處理更簡單、風險更低。

- **字卡瀏覽**（`subtab-vocab-flashcard`）：純瀏覽，依分類篩選 + 隨機排序，點卡片翻面看中文意思+例句，不記錄任何進度
- **測驗練習**（`subtab-vocab-quiz`）：依分類 / 題數 / 是否只考待加強單字設定，逐題作答＋對答案＋詳解
  - **單字測驗題生成邏輯**（`generateVocabQuestion()`）：每次即時生成，非預先寫死。50% 機率考「英文字選中文意思」，50% 機率考「例句挖空選正確單字」（若該字的 `example` 抓不到 `word` 原形字串則強制退回字義題）；干擾選項優先從同分類的其他單字抽，不夠再從全部單字庫補
  - 獨立的 localStorage：`toeic_vocab_attempts`（作答紀錄）、`toeic_vocab_wrongbook`（待加強單字，規則同 RC 錯題本：答錯加入、連續答對 2 次移除）、`toeic_vocab_custom`（使用者自行匯入的單字）
  - 待加強單字清單顯示在單字分頁內（不在全站的「錯題本」分頁裡）

## 已知限制 / 之後可以做的事

- 聽力（LC，Part 1-4）完全沒做，之後要做需要 TTS 或錄音素材，是完全獨立的一塊
- 估算分數只是粗略內插，不是精準預測
- 沒有帳號系統/雲端同步，僅限單一瀏覽器本機使用
- RC 題庫量、單字量對「三個月衝 990」這種目標來說仍偏少，需要持續擴充（可用 AI 依本文件的 schema 大量生成新題/新字，或使用者自行匯入）
- 高中7000單 Level 1（1,026 字）已完成，但 Level 2-6（約 5,400 字）都還沒做，別誤以為 7000 字已經匯完
- 單字測驗目前完全獨立於 RC 的儀表板/模考之外；如果之後想要「總覽儀表板」把單字正確率也放進同一張圖，或想讓模考也混入單字題，是尚未做的新範疇，別誤以為已經接好
